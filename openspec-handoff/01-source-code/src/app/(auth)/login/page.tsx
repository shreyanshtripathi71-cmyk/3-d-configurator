import LoginForm from "@/components/LoginForm";

export const metadata = {
  title: "Login | Newkore Portal",
  description: "Sign in to the Newkore dealer and factory portal.",
};

export default function LoginPage() {
  return <LoginForm />;
}
